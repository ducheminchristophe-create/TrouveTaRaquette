import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Permet d'importer les fichiers JSON depuis src/data
  // Pas besoin de config spéciale — Next.js supporte les imports JSON nativement
}

export default nextConfig
